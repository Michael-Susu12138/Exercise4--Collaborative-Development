using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnikScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // My name is Anik Barua. 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
